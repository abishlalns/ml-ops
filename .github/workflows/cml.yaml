name: MLOps-demo
on:
  push:
    paths:
      - "front-end/**"
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Train model
        run: |
          python front-end/test.py
